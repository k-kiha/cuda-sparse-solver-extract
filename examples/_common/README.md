# Example Common Code

This directory contains shared support code for examples. It is not a core
solver implementation and it is not a standalone runnable example.

The current shared code is the Fortran `bind(C)` adapter under `fortran/`.
