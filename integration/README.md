# Integration References

This directory is for project-integration evidence that is useful context but
not part of the minimal solver smoke tests.

`cupid_gfortran_bridge` shows how a CUPID-like `gfortran` application can call
an NVHPC-built CUDA solver layer through a small bridge.
